{
  "name": "p6-ci-application-java-release-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Release tooling only (semantic-release). Not the application itself — the Java app is built with Gradle.",
  "devDependencies": {
    "semantic-release": "^24.2.9",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/release-notes-generator": "^14.0.3",
    "@semantic-release/github": "^11.0.1",
    "@semantic-release/exec": "^7.0.3"
  }
}